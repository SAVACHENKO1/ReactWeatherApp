import React from "react";

const Title =()=>(
        <div>
            <h1 className="titles container__title">My Weather App</h1>
            <h3 className="title container__subtitle">simple general app for weater</h3>
        </div>
    );

export default Title;